import 'package:flutter/material.dart';

class AppTheme {
  ThemeData get defaultTheme => ThemeData.from(
        // colorScheme: ColorScheme.fromSeed(seedColor: Color(0xFF19686A)),
        colorScheme: ColorScheme.fromSeed(seedColor: const Color(0xFF5A6055)),
        useMaterial3: true,
      );
}
